version: "3.9"

services:
  Cognix:
    image: interview-ai:latest
    container_name: Cognix
    ports:
      - "8000:8000"
    volumes:
      - cognix_data:/app/data
    restart: unless-stopped

volumes:
  cognix_data:
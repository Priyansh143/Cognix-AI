version: "3.9"

services:
  interviewsim:
    image: interview-ai:latest
    container_name: interviewsim
    ports:
      - "8000:8000"
    volumes:
      - interviewsim_data:/app/data
    restart: unless-stopped

volumes:
  interviewsim_data: